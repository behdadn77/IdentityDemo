@using Microsoft.Extensions.Options
@inject IOptions<IdentityDemo.Options.ReCaptchav3Options> options
<script src="https://www.google.com/recaptcha/api.js?render=@options.Value.SiteKey"></script>
<script>
    grecaptcha.ready(function () {
        grecaptcha.execute('@options.Value.SiteKey', { action: 'homepage' }).then(function (token) {
            // Add your logic to submit to your backend server here.
            console.log(token)
            $('#Input_ReCaptchaToken').val(token);
        });
    });
</script>